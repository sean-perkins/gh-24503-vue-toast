import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-toast-24503',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
